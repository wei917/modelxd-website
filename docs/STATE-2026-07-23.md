# ModelXD — state snapshot, July 23 2026 (eve of launch)

Working-tree summary for continuing work in a fresh session. Everything
below is implemented, tsc-clean, and verified on localhost — but NOT yet
committed/pushed (deployed build is still commit `7147c9a`).

## Launch blockers
1. Commit & push to `dev` (60+ changed files since 7147c9a), redeploy.
2. Vercel env: add `XAI_API_KEY`, `RUNWAYML_API_SECRET` (ANTHROPIC_API_KEY done).
3. Stripe (US LLC): confirm live-mode activation + payouts, enable JCB,
   register production webhook + live signing secret in Vercel.
4. Run `supabase/49_remove_daily_credit.sql` if not yet run (50 is run).
5. Decide SITE_PASSWORD gate for modelxd.com on launch day.

## Providers (lib/providers/)
openai · google · alibaba · xai (image `/v1/images/*` + video `/v1/videos/generations`,
image field MUST be `{type:'image_url', url}`) · anthropic (Messages SSE, text)
· runway (X-Runway-Version 2024-11-06; ratios exactly `1280:720|720:1280`;
duration ≤10s; poll `/v1/tasks/{id}`). All live-tested with real generations.

## Catalog changes (DB `ai_models`)
Added: gpt-5.6-sol/terra/luna; claude-fable-5, claude-opus-4-8, claude-sonnet-5;
gemini-3.6-flash, gemini-3.5-flash-lite; grok-imagine-image; grok-imagine-video-1.5;
runway gen4.5 + gen4_turbo. Fixes: gpt-5.5 cached_input 0.5; flash-lite negative
rate → 1.5; Omni Flash = 720p only, 3–10s selectable (`response_format.duration`).
Runway resells third-party models — intentionally NOT listed (markup breaks
price honesty).

## Features this sprint
- Thinking levels end-to-end (OpenAI `reasoning.effort`, Gemini
  `thinkingConfig.thinkingLevel`); per-level pricing honored; text ⚙ panel.
- Error report refs: failed slots show `Ref: <8 chars>` = provider_calls.request_id.
- Delete account (profile Danger Zone + /api/profile/delete-account) — GDPR path.
- /terms + /privacy drafted (owner review pending); links in sidebar footer.
- Daily $1 credit REMOVED (free XDuels are the free tier); grants default
  description "Gift from a friend".
- XBoard: "Score" (blind-vote quality rating) column beside XD Score.
- TopBar removed; in-page `// PAGE` eyebrow + big headline per page.
- XCreate: discount corner tags on slots; slots always fill A→D (compact on
  remove, leftmost on add); fixed slot widths; single ⚙ toggles all configs;
  shared text templates with XDuel; slider-drag remount bug fixed (OptPill/
  OptGroup hoisted to module scope).
- i18n sweep: profile tabs = nav names, 模型 A–D, Share/Delete, shared
  ModeIcon selector across XCreate/XDuel/XVote.

## Post-launch queue
Seedance direct (BytePlus account needed, ~$0.01–0.02/s); read-only ModelXD
MCP server/agent skill exposing XBoard; XDuel job architecture (SSE dies on
browser disconnect); provider health auto-disable; Anthropic/Qwen thinking;
Omni `extend` task; XVote report button; og:image + robots + sitemap; Sentry.

## Positioning (owner-approved wording)
Official: "ModelXD is an AI content creation service that automatically
selects the most suitable AI model for each user's task, generating text,
images, and video at the best available quality and price based on the
results of community blind comparisons." Long-term: benchmarked routing
agent (duels/votes are its training data). No em dashes in official copy.

## July 24 (launch day) updates
- Kimi K3 via new moonshot provider (lib/providers/moonshot.ts): OpenAI-compat
  SSE at api.moonshot.ai/v1, thinking:{effort:'low|high|max'}, streams only
  delta.content (skips delta.reasoning_content). Live-verified.
  MOONSHOT_API_KEY set locally; still needed in Vercel env.
- Claude thinking wired (Fable 5 / Sonnet 5 / Opus 4.8): API rejects
  budget_tokens; correct shape is thinking:{type:'adaptive'} +
  output_config:{effort} with low/medium/high/xhigh/max (probed live, then
  confirmed by platform.claude.com/docs/en/build-with-claude/effort; default
  high; changing effort invalidates prompt cache; Fable 5 max_tokens caps
  thinking+text combined). DB rows declare thinking_levels so the Thinking
  config group renders.
- PDF input for ALL text models: lib/pdf-extract.ts (unpdf) folds extracted
  text (up to 200k chars) for non-native models; native PDF only for
  openai/google models declaring pdf_to_text. XCreate estimator now counts
  doc tokens (min(fileSize/4, 50k) per pdf/txt attachment).
- PDF-too-large handling (refs feead46c / 689e34a3 were context-exceeded on a
  927-page PDF, roughly 900k+ tokens):
  - provider-errors.ts TOOBIG mapping -> "The attached file or prompt is too
    large for this model. Try a smaller file or a model with a larger context
    window."
  - pdf-extract.ts estimatePdfTokens (pages*258 + textChars/4);
    providers/index.ts NATIVE_PDF_TOKEN_LIMITS {openai:400k, google:1M} fails
    fast in resolveDocAttachments before the provider call.
  - OPEN: owner may prefer auto text-extraction fallback for huge PDFs on
    native models instead of rejecting. Not decided.
