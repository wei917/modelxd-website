// app/xtalk/WerewolfRoom.tsx — REMOVED (CC, Aug 1 2026)
//
// The client-side moderator. Replaced by WerewolfLive + /api/xtalk/werewolf,
// which hold the game on the server so a human can play without being able
// to read the roles out of this page. Kept as a stub rather than deleted so
// nobody wires it back in by accident: it recorded results to
// /api/xtalk/game, and that path is now dead — the server route records.
export {}
