# dependencies
node_modules/
package-lock.json

# next.js
.next/
out/
next-env.d.ts

# env files
.env
.env.*

# build
tsconfig.tsbuildinfo

# misc
.DS_Store
*.tgz
*.zip

# scratch / fixtures (local-only)
outputs/
# Root-anchored: this is the local scratch dir. Without the leading slash
# it also swallowed public/samples/, so the bundled template sample files
# were never committed and 404'd in production (caught July 25, 2026).
/samples/
supabase/.temp/
_to_delete/
public/templates/_raw/
商標申請資料包.md
